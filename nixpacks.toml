[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gnumake", "gcc"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node --no-warnings --expose-gc --max-old-space-size=512 --experimental-global-webcrypto index.js"
